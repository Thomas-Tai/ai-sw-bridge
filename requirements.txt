# Runtime dependencies for ai-sw-bridge.
#
# pyproject.toml [project.dependencies] is the source of truth; this file
# mirrors it for the conventional `pip install -r requirements.txt` path.
# Keep the two in sync. Windows-only: pywin32 and SOLIDWORKS are Windows-only.
pywin32>=305,<400
Pillow>=12.2.0,<13
oletools>=0.60,<1
jsonschema>=4.0,<5
numpy>=1.24,<3
sqlite-vec>=0.1,<0.2
